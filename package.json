{
  "name": "my-cypress-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:split": "npx cypress-split --ci-build-id $CI_BUILD_ID --split $TOTAL_NODES --index $NODE_INDEX"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.2.0",
    "cypress-split": "^1.24.14"
  },
  "dependencies": {
    "axios": "^1.8.4"
  }
}
